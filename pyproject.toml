[project]
name = "lzits-nodes"
description = "ComfyUI custom nodes for text/string manipulation, LoRA selection, image outpainting, YOLO object detection, and interactive bounding box drawing for object-move workflows."
version = "1.1.3"
license = { file = "LICENSE" }
dependencies = ["ultralytics"]

[project.urls]
Repository = "https://github.com/lzitser23/lzits-nodes"  

[tool.comfy]
PublisherId = "lzitser"  
DisplayName = "lzits Nodes"
Icon = "" # add later if i want
