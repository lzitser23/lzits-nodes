[project]
name = "lzits-nodes"
description = "ComfyUI custom nodes for text/string manipulation including splitting strings into batches, appending/prepending text, and picking items from lists by index."
version = "1.0.0"
license = { file = "LICENSE" }
dependencies = []

[project.urls]
Repository = "https://github.com/OWNER/REPO"  # TODO: Replace with your actual GitHub repository URL

[tool.comfy]
PublisherId = ""  # TODO: Fill in your Publisher ID from Comfy Registry
DisplayName = "LZITS Nodes"
Icon = ""